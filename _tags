#default directories used by Dedukti
"kernel" or "utils" or "parser": include

#only applied for dkcheck, dktop etc...
not <*.cmxa>: use_menhir, use_unix

#applied for every compiled module
<*/*.cmx>: inline(11), optimize(3), bin_annot, for-pack(Dedukti)
