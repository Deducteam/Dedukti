(executable
        (name dkcheck)
        (public_name dkcheck)
	(modules dkcheck)
        (libraries kernel parse api))

(executable
        (name dkdep)
        (public_name dkdep)
	(modules dkdep)
        (libraries kernel parse api))

(executable
        (name dktop)
        (public_name dktop)
	(modules dktop)
        (libraries kernel parse api))

(executable
        (name dkprune)
        (public_name dkprune)
	(modules dkprune)
        (libraries kernel parse api))
