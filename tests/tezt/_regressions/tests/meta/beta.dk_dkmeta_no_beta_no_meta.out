tests/tezt/_regressions/tests/meta/beta.dk_dkmeta_no_beta_no_meta.out

./dkmeta.native -nc -q --no-beta --no-meta tests/meta/beta.dk
def A : Type.

B : Type.

[] A --> B.

def f : A -> A := y:A => (x:A => x) y.

