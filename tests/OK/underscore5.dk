(; OK ;)

A : Type.
def B : (A -> A) -> Type.

[] B (x => _ x) --> A.
(; This is equivalent to  B (x => X x) --> A  (with X fresh) ;)

(;# assert  B (x => x) == A #;)

a : A.
(;# assert  B (x => a) == A #;)
