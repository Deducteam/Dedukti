(; OK ;)

A : Type.
def B : (A -> A -> A) -> Type.

[] B (x => y => _ x) --> A.
(; This is equivalent to  B (x => y => X x) --> A  (with X fresh) ;)

a : A.
(;# assert  B (_ => _ => a) == A #;)

(;# assert  B (u => v => u) == A #;)

(;# assertnot  B (u => v => v) == A #;)
