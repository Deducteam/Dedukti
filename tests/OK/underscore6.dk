(; OK ;)

A : Type.
def B : (A -> A) -> Type.

[] B (_ => _) --> A.
(; This is equivalent to  B (x => X) --> A    (with x and X fresh) ;)

a : A.
(;# assert  B (y => a) == A #;)

(;# assert  (z:A => B (y => z)) == (z:A => A) #;)

(;# assertnot  B (y => y) == A #;)
