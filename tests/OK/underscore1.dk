(; OK ;)

A : Type.
a : A.

def B : (A -> A) -> Type.

b : B (_ => a).
(; This is equivalent to  b : B (x => a);)

[] B (_ => a) --> A.
(; This is equivalent to  B (x => a) --> A ;)

[] B (_ => _) --> A.
(; This is equivalent to  B (x => X) --> A    (with X fresh) ;)

[] B (x => _ x) --> A.
(; This is equivalent to  B (x => X x) --> A  (with X fresh) ;)
