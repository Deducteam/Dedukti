(; OK -q ;)

A : Type.
B : A -> Type.
b : a:A -> B a.

def c : f : (A -> A) -> (x: A -> y : A -> B (f x)) -> Type.

[F,G] c F (x => y => b (G y)) --> A.

(;
Inferred constraints:
  B (F x)  ==  B (G y)
is dropped
;)
