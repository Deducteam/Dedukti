(ocamllex
 (modules lexer))
(menhir
 (modules menhir_parser)
 (flags (--external-tokens Tokens)))

(ocamllex
 (modules sukerujo_lexer))

(menhir
 (modules sukerujo_parser)
 (flags (--external-tokens Sukerujo_tokens)))

(library
 (name parsers)
 (public_name dedukti.parsers)
 (libraries kernel))
